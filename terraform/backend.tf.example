# Copy to backend.tf and create the S3 bucket + DynamoDB table for state locking first.
#
# terraform {
#   backend "s3" {
#     bucket         = "gardenia-terraform-state"
#     key            = "alpha/terraform.tfstate"
#     region         = "eu-west-1"
#     encrypt        = true
#     dynamodb_table = "gardenia-terraform-locks"
#   }
# }
